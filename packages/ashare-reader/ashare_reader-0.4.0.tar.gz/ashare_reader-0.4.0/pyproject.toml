[project]
name = "ashare-reader"
version = "0.4.0"
description = "读取本地股票数据"
readme = "README.md"
authors = [
    { name = "zw", email = "szzend@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.3",
    "pyarrow>=19.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
