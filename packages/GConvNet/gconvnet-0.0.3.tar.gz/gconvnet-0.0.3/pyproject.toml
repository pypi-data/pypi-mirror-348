[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "GConvNet"
version = "0.0.3"
authors = [
  { name="sobazino", email="sobazino@gmail.com" }
]
description = "GConvNet"
readme = "README.md"
license = {text = "Apache-2.0 license"}
classifiers = [
    "Programming Language :: Python :: 3",
]