[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "debby_root"
version = "0.1.0"
description = "Ein CLI-Wrapper für debootstrap"
authors = [
    { name="Calvin Ronksley", email="hexzhen3x7@blackzspace.de" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"

[project.scripts]
debby_root = "debby_root.cli:main"
