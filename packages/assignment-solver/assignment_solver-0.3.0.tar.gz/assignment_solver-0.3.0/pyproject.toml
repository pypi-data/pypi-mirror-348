[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
bindings = "cffi"
compatibility = "linux"

[project]
name = "assignment_solver"
version = "0.3.0"
description = "Rust-based assignment solver for Python"
authors = [{name = "Your Name"}]
requires-python = ">=3.8"
