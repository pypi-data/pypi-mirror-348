[project]
name = "netcheckup"
version = "0.1.0"
description = "A simple internet health diagnostic tool"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [{name = "AkhilSu", email = "akhilsudhakaran@outlook.com"}]
dependencies = []

[project.scripts]
netcheckup = "netcheckup:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
