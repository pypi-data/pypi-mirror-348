[project]
name = "aapt-mcp-server"
version = "0.1.0"
description = "An MCP server that provides APK analysis capabilities using the Android Asset Packaging Tool (aapt)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.9.0",
]
[project.scripts]
aapt-mcp-server="main:main"