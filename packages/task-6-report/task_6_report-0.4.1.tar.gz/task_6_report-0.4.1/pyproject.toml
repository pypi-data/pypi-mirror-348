[project]
name = "task_6_report"
version = "0.4.1"
description = "Аналізатор гонки F1"
authors = [
    { name = "Yurii_Step", email = "step.crypt@email.com" },
]
requires-python = ">=3.9"
dependencies = []


license = { text = "MIT" }


readme = "README.md"


[project.scripts]
race-report = "race_report.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]