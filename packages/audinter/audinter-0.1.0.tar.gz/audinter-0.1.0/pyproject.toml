[tool.poetry]
name = "audinter"
version = "0.1.0"
description = "Interpretability and complexity metrics for ML models."
authors = ["Antónia Brito"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
