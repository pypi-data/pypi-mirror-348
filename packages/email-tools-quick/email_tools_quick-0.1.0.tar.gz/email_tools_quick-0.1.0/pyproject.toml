[project]
name = "email-tools-quick"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4",
    "curl-cffi",
    "loguru",
    "python-dateutil",
]
