[project]
name = "email-tools-quick"
version = "0.1.1"
description = "A collection of tools for email processing"
requires-python = ">=3.10"
readme = "README.md"
dependencies = [
    "beautifulsoup4",
    "curl-cffi",
    "loguru",
    "python-dateutil",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.1.0",
]
