from utlib.numbers import digit_sum, average

# Printing the sum of each number
print(digit_sum(12345))

# Returning average float with decimal=int point
values = [1, 23, 4,]
print(average(values=values, decimal=27,))
