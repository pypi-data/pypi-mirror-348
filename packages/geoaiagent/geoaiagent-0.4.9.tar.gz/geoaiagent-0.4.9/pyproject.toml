[project]
name = "geoaiagent"
version = "0.4.9"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pathlib","mcp","pandas"]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
geoaiagent = "geoaiagent.__main__:main"