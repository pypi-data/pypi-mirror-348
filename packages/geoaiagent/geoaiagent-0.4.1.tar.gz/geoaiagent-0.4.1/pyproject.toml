[project]
name = "geoaiagent"
version = "0.4.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["mcp","pandas","openpyxl","pathlib","xgboost","sklearn"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
geoaiagent = "geoaiagent.__main__:thisismain"

