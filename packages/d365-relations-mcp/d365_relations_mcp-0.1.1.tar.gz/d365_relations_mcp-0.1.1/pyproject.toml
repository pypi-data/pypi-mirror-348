[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "d365-relations-mcp"
version = "0.1.1"
description = "MCP server for Dynamics 365 Table Relationship Explorer"
authors = [
    {name = "duggurd"}
]
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.1.0",
    "d365-relations-cli>=0.1.1"
]

[project.scripts]
d365-relations-mcp = "main:main" 