from setuptools import setup, find_packages

setup(
    name='cse_code',
    version='2.0',
    packages=find_packages(),
    install_requires=[],  # Add dependencies if needed
    author='naveenthegreat',
    description='A CSE package',
    classifiers=[
        'Programming Language :: Python :: 3',
    ],
)
