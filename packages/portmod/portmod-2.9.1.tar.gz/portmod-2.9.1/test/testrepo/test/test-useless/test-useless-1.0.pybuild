from pybuild import Pybuild1


class Package(Pybuild1):
    NAME = "Test"
    KEYWORDS = "test"
    LICENSE = "test"
    DESC = "test desc"
    HOMEPAGE = "https://example.org"
    RDEPEND = ""
    IUSE = "foo bar baz"
    REQUIRED_USE = "^^ ( foo bar ) || ( bar baz ) foo !baz"
