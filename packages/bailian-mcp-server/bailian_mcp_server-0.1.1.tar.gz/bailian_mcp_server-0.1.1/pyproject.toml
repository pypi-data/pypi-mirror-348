[project]
name = "bailian-mcp-server"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bs4>=0.0.2",
    "dashscope>=1.23.3",
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "mcp[cli]>=1.9.0",
]

[project.scripts]
bailian-mcp-server = "bailian_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
