[project]
name = "hequate_common"
version = "0.1.0"

[tool.poetry]
name = "hequate_common"
version = "0.1.0"
description = ""
authors = ["Hequate Software Team"]
packages = [
   {include = "hequate_common", from = ".."}
]

[tool.poetry.dependencies]
python = "3.12.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
