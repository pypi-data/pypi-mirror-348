#!/usr/bin/env bash

ARCH=x86_64
MODSUBDIR=x86_64

if test "x${NRNHOME}" = x ; then
	prefix=/var/folders/g4/q3s03t8531x2m822x7q8y6l40000gn/T/tmpqxx38a5d/wheel/platlib
	exec_prefix=/var/folders/g4/q3s03t8531x2m822x7q8y6l40000gn/T/tmpqxx38a5d/wheel/platlib
	NRNBIN=${exec_prefix}/bin/
else
    prefix="$NRNHOME"
    exec_prefix="${prefix}"
	NRNBIN="${exec_prefix}/bin/"
fi

if test "${NRNHOME_EXEC}" != "" ; then
	exec_prefix="${NRNHOME_EXEC}"
	NRNBIN="${exec_prefix}/bin/"
fi

NRNIV="${NRNBIN}nrniv"
NEURONHOME="${prefix}/share/nrn"
export NEURONHOME

if [ -x ${MODSUBDIR}/special ] ; then
	program="./${MODSUBDIR}/special"
else
	program="${NRNIV}"
fi

exec "$program" "$NEURONHOME/lib/hoc/nrngui.hoc" "$@" -

