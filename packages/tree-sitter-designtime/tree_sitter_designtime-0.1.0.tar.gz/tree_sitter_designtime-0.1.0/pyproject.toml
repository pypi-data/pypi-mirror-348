[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "tree-sitter-designtime"
description = "Tree-sitter grammar for design time files"
readme = "README.md"
requires-python = ">=3.7"
dynamic = ["version"]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages]
find = {}

[tool.setuptools_scm]
