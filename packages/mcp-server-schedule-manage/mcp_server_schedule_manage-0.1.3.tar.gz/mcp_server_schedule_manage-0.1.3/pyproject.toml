[project]
name = "mcp-server-schedule-manage"
version = "0.1.3"
description = "A simple MCP schedule manage server"
readme = "README.md"
requires-python = ">=3.10"
authors = [
{name = "hechangtian", email="15993091621@163.com"}
]
dependencies = [
]

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
mcp_server_schedule_manage = "mcp_server_schedule_manage:main"