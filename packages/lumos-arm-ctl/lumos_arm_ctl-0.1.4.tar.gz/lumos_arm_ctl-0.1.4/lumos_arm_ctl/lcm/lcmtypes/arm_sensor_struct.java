/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 */

package lcmtypes;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class arm_sensor_struct implements lcm.lcm.LCMEncodable
{
    public int runModeVec[];
    public float curCurrentVec[];
    public float curSpeedVec[];
    public float curJointPosVec[];
    public int curErrCodeVec[];
    public int isUsed[];
 
    public arm_sensor_struct()
    {
        runModeVec = new int[14];
        curCurrentVec = new float[14];
        curSpeedVec = new float[14];
        curJointPosVec = new float[14];
        curErrCodeVec = new int[14];
        isUsed = new int[14];
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x22d827812d1966faL;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(lcmtypes.arm_sensor_struct.class))
            return 0L;
 
        classes.add(lcmtypes.arm_sensor_struct.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        for (int a = 0; a < 14; a++) {
            outs.writeInt(this.runModeVec[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeFloat(this.curCurrentVec[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeFloat(this.curSpeedVec[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeFloat(this.curJointPosVec[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeInt(this.curErrCodeVec[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeInt(this.isUsed[a]); 
        }
 
    }
 
    public arm_sensor_struct(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public arm_sensor_struct(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static lcmtypes.arm_sensor_struct _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        lcmtypes.arm_sensor_struct o = new lcmtypes.arm_sensor_struct();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.runModeVec = new int[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.runModeVec[a] = ins.readInt();
        }
 
        this.curCurrentVec = new float[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.curCurrentVec[a] = ins.readFloat();
        }
 
        this.curSpeedVec = new float[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.curSpeedVec[a] = ins.readFloat();
        }
 
        this.curJointPosVec = new float[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.curJointPosVec[a] = ins.readFloat();
        }
 
        this.curErrCodeVec = new int[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.curErrCodeVec[a] = ins.readInt();
        }
 
        this.isUsed = new int[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.isUsed[a] = ins.readInt();
        }
 
    }
 
    public lcmtypes.arm_sensor_struct copy()
    {
        lcmtypes.arm_sensor_struct outobj = new lcmtypes.arm_sensor_struct();
        outobj.runModeVec = new int[(int) 14];
        System.arraycopy(this.runModeVec, 0, outobj.runModeVec, 0, 14); 
        outobj.curCurrentVec = new float[(int) 14];
        System.arraycopy(this.curCurrentVec, 0, outobj.curCurrentVec, 0, 14); 
        outobj.curSpeedVec = new float[(int) 14];
        System.arraycopy(this.curSpeedVec, 0, outobj.curSpeedVec, 0, 14); 
        outobj.curJointPosVec = new float[(int) 14];
        System.arraycopy(this.curJointPosVec, 0, outobj.curJointPosVec, 0, 14); 
        outobj.curErrCodeVec = new int[(int) 14];
        System.arraycopy(this.curErrCodeVec, 0, outobj.curErrCodeVec, 0, 14); 
        outobj.isUsed = new int[(int) 14];
        System.arraycopy(this.isUsed, 0, outobj.isUsed, 0, 14); 
        return outobj;
    }
 
}

