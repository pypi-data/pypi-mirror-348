"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class testing_tools_package(object):
    __slots__ = ["timestamp", "command", "message"]

    def __init__(self):
        self.timestamp = 0
        self.command = ""
        self.message = ""

    def encode(self):
        buf = BytesIO()
        buf.write(testing_tools_package._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.timestamp))
        __command_encoded = self.command.encode('utf-8')
        buf.write(struct.pack('>I', len(__command_encoded)+1))
        buf.write(__command_encoded)
        buf.write(b"\0")
        __message_encoded = self.message.encode('utf-8')
        buf.write(struct.pack('>I', len(__message_encoded)+1))
        buf.write(__message_encoded)
        buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != testing_tools_package._get_packed_fingerprint():
            raise ValueError("Decode error")
        return testing_tools_package._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = testing_tools_package()
        self.timestamp = struct.unpack(">q", buf.read(8))[0]
        __command_len = struct.unpack('>I', buf.read(4))[0]
        self.command = buf.read(__command_len)[:-1].decode('utf-8', 'replace')
        __message_len = struct.unpack('>I', buf.read(4))[0]
        self.message = buf.read(__message_len)[:-1].decode('utf-8', 'replace')
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if testing_tools_package in parents: return 0
        tmphash = (0x2dae1f15f4aea7) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if testing_tools_package._packed_fingerprint is None:
            testing_tools_package._packed_fingerprint = struct.pack(">Q", testing_tools_package._get_hash_recursive([]))
        return testing_tools_package._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

