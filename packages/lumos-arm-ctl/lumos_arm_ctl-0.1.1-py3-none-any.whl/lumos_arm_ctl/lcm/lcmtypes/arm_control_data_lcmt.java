/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 */

package lcmtypes;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class arm_control_data_lcmt implements lcm.lcm.LCMEncodable
{
    public float joint_curPos[];
    public float joint_curVel[];
    public float joint_curCurrent[];
    public float stats[];
    public float shoulder_pos[];
    public float elbow_pos[];
    public float hand_pos[];
 
    public arm_control_data_lcmt()
    {
        joint_curPos = new float[14];
        joint_curVel = new float[14];
        joint_curCurrent = new float[14];
        stats = new float[14];
        shoulder_pos = new float[12];
        elbow_pos = new float[12];
        hand_pos = new float[12];
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0xc96c437beb204d8bL;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(lcmtypes.arm_control_data_lcmt.class))
            return 0L;
 
        classes.add(lcmtypes.arm_control_data_lcmt.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        for (int a = 0; a < 14; a++) {
            outs.writeFloat(this.joint_curPos[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeFloat(this.joint_curVel[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeFloat(this.joint_curCurrent[a]); 
        }
 
        for (int a = 0; a < 14; a++) {
            outs.writeFloat(this.stats[a]); 
        }
 
        for (int a = 0; a < 12; a++) {
            outs.writeFloat(this.shoulder_pos[a]); 
        }
 
        for (int a = 0; a < 12; a++) {
            outs.writeFloat(this.elbow_pos[a]); 
        }
 
        for (int a = 0; a < 12; a++) {
            outs.writeFloat(this.hand_pos[a]); 
        }
 
    }
 
    public arm_control_data_lcmt(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public arm_control_data_lcmt(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static lcmtypes.arm_control_data_lcmt _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        lcmtypes.arm_control_data_lcmt o = new lcmtypes.arm_control_data_lcmt();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.joint_curPos = new float[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.joint_curPos[a] = ins.readFloat();
        }
 
        this.joint_curVel = new float[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.joint_curVel[a] = ins.readFloat();
        }
 
        this.joint_curCurrent = new float[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.joint_curCurrent[a] = ins.readFloat();
        }
 
        this.stats = new float[(int) 14];
        for (int a = 0; a < 14; a++) {
            this.stats[a] = ins.readFloat();
        }
 
        this.shoulder_pos = new float[(int) 12];
        for (int a = 0; a < 12; a++) {
            this.shoulder_pos[a] = ins.readFloat();
        }
 
        this.elbow_pos = new float[(int) 12];
        for (int a = 0; a < 12; a++) {
            this.elbow_pos[a] = ins.readFloat();
        }
 
        this.hand_pos = new float[(int) 12];
        for (int a = 0; a < 12; a++) {
            this.hand_pos[a] = ins.readFloat();
        }
 
    }
 
    public lcmtypes.arm_control_data_lcmt copy()
    {
        lcmtypes.arm_control_data_lcmt outobj = new lcmtypes.arm_control_data_lcmt();
        outobj.joint_curPos = new float[(int) 14];
        System.arraycopy(this.joint_curPos, 0, outobj.joint_curPos, 0, 14); 
        outobj.joint_curVel = new float[(int) 14];
        System.arraycopy(this.joint_curVel, 0, outobj.joint_curVel, 0, 14); 
        outobj.joint_curCurrent = new float[(int) 14];
        System.arraycopy(this.joint_curCurrent, 0, outobj.joint_curCurrent, 0, 14); 
        outobj.stats = new float[(int) 14];
        System.arraycopy(this.stats, 0, outobj.stats, 0, 14); 
        outobj.shoulder_pos = new float[(int) 12];
        System.arraycopy(this.shoulder_pos, 0, outobj.shoulder_pos, 0, 12); 
        outobj.elbow_pos = new float[(int) 12];
        System.arraycopy(this.elbow_pos, 0, outobj.elbow_pos, 0, 12); 
        outobj.hand_pos = new float[(int) 12];
        System.arraycopy(this.hand_pos, 0, outobj.hand_pos, 0, 12); 
        return outobj;
    }
 
}

