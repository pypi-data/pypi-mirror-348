/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 */

package lcmtypes;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class magic_doll_data_struct implements lcm.lcm.LCMEncodable
{
    public int n_dof;
    public float pos_now[];
    public float vel_now[];
    public float current_now[];
 
    public magic_doll_data_struct()
    {
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x04874d431ea64c8fL;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(lcmtypes.magic_doll_data_struct.class))
            return 0L;
 
        classes.add(lcmtypes.magic_doll_data_struct.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        outs.writeInt(this.n_dof); 
 
        for (int a = 0; a < this.n_dof; a++) {
            outs.writeFloat(this.pos_now[a]); 
        }
 
        for (int a = 0; a < this.n_dof; a++) {
            outs.writeFloat(this.vel_now[a]); 
        }
 
        for (int a = 0; a < this.n_dof; a++) {
            outs.writeFloat(this.current_now[a]); 
        }
 
    }
 
    public magic_doll_data_struct(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public magic_doll_data_struct(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static lcmtypes.magic_doll_data_struct _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        lcmtypes.magic_doll_data_struct o = new lcmtypes.magic_doll_data_struct();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.n_dof = ins.readInt();
 
        this.pos_now = new float[(int) n_dof];
        for (int a = 0; a < this.n_dof; a++) {
            this.pos_now[a] = ins.readFloat();
        }
 
        this.vel_now = new float[(int) n_dof];
        for (int a = 0; a < this.n_dof; a++) {
            this.vel_now[a] = ins.readFloat();
        }
 
        this.current_now = new float[(int) n_dof];
        for (int a = 0; a < this.n_dof; a++) {
            this.current_now[a] = ins.readFloat();
        }
 
    }
 
    public lcmtypes.magic_doll_data_struct copy()
    {
        lcmtypes.magic_doll_data_struct outobj = new lcmtypes.magic_doll_data_struct();
        outobj.n_dof = this.n_dof;
 
        outobj.pos_now = new float[(int) n_dof];
        if (this.n_dof > 0)
            System.arraycopy(this.pos_now, 0, outobj.pos_now, 0, this.n_dof); 
        outobj.vel_now = new float[(int) n_dof];
        if (this.n_dof > 0)
            System.arraycopy(this.vel_now, 0, outobj.vel_now, 0, this.n_dof); 
        outobj.current_now = new float[(int) n_dof];
        if (this.n_dof > 0)
            System.arraycopy(this.current_now, 0, outobj.current_now, 0, this.n_dof); 
        return outobj;
    }
 
}

