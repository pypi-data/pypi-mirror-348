[project]
name = "fenda-weather"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.0",
    "setuptools>=80.7.1",
    "twine>=6.1.0",
    "wheel>=0.45.1",
]

[project.scripts]
fenda_weather = "fenda_weather.server:main" # 指定默认执行的方法