[project]
name = "regex-pattern-index"
version = "0.1.0"
description = "WIP index of regex patterns."
readme = "README.md"
authors = [{ name = "Hart Traveller", email = "ruling_fiction.0b@icloud.com" }]
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = [
    "hypothesis>=6.131.18",
    "pytest>=8.3.5",
]

[tool.hatch.build.targets.wheel]
packages = ["rpi"]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
