[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "DecoratedText"
version = "1.0"
autors = [
    { name="Sora" }
]
description = "It's a great library for text design!"
readme = "README.md"
requires-python = ">=3.5.0"
classifiers = [
   "Programming Language :: Python :: 3",
   "License :: OSI Approved :: MIT License",
   "Operating System :: OS Independent",
]