from setuptools import setup, find_packages

setup(
    name='dp-qtools',
    version='0.3',
    packages=find_packages(),
    install_requires=[
    ],
    long_description="qtools is a library of functions used in the Datapod framework"
)