[project]
name = "poffertjes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Silvio Lugaro", email = "silvio.lugaro@gruppoiren.it" }
]
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "marimo>=0.13.10",
    "pandas>=2.2.3",
    "ruff>=0.11.10",
]

[project.scripts]
poffertjes = "poffertjes:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
